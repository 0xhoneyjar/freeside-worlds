# Idempotency Report — first-pass generator vs live infrastructure

**Date**: 2026-04-28 (initial bootstrap session)
**Scope**: 5 existing worlds (apdao, mibera, midi, rektdrop, score-api)
**Verdict**: ⚠️ partial — cosmetic-clean, but **semantic diffs require terraform state migration** before generator output can replace live `.tf` files.

This report documents what the generator produces vs what's live in `loa-freeside/infrastructure/terraform/world-{name}.tf` today, and how to get from "partial" to "zero-diff `terraform plan`."

---

## TL;DR

| world | cosmetic diff | semantic diff | resource recreation if applied as-is? |
|---|---|---|---|
| apdao | ✅ comment + whitespace only | none | no — clean |
| rektdrop | ✅ comment + whitespace only | none (subdomain output now matches after fix) | no — clean |
| midi | ✅ comment + whitespace only | secret naming path | yes — secrets recreated |
| mibera | ✅ comment + whitespace only | secret naming path + resource address (`mibera` vs `honeyroad`) | yes — secrets recreated + resource rename |
| score-api | ✅ comment + whitespace only | secret resource pattern (literal vs for_each) + name path + missing secret_string | yes — secret destroyed and recreated WITHOUT VALUE |

**apdao + rektdrop are clean.** The other three need migration work before the generator output can be adopted.

---

## What's idempotent (cosmetic-only — terraform plan unaffected)

These differences exist between generated and live `.tf`, but produce **zero terraform-plan changes**:

1. **Banner comments**: generated file declares "Auto-generated by ..."; live files have hand-written historical context (B2 migration notes, mibera-honeyroad rationale). Comments don't drive resource state.
2. **Section dividers**: live files use `# ----- Section -----` style; generator strips these.
3. **Inline rationale comments**: live `world-mibera.tf` has multi-line comments before each block (CPU rationale, health-check rationale, secrets-absent rationale). Generator strips.
4. **Whitespace alignment**: live files align the shared-infra block at 24 chars; generator at 25. `env_vars` block: live uses extra padding for value alignment; generator emits canonical single-space.
5. **Output `description =` field**: generator adds `description = "..."` to outputs that previously had only `value =`. Description is metadata, not resource state.

**These differences are accepted and will converge to canonical form on first migration.**

---

## What's NOT idempotent (semantic — would change resource state)

### 1. score-api secret resource pattern (HIGH IMPACT)

**Live** `world-score-api.tf`:
```hcl
resource "aws_secretsmanager_secret" "score_api_db_url" {
  name = "${local.name_prefix}/world-score-api/database-url"
  ...
}
resource "aws_secretsmanager_secret_version" "score_api_db_url" {
  secret_id     = aws_secretsmanager_secret.score_api_db_url.id
  secret_string = "postgresql://..." # templated from RDS resources
}
```

**Generated** `world-score-api-secrets.tf`:
```hcl
resource "aws_secretsmanager_secret" "score_api" {
  for_each = local.score_api_secrets  # toset(["database_url"])
  name     = "${local.name_prefix}/worlds/score-api/database_url"
  ...
}
```

**Differences**:
- Resource address changes: `aws_secretsmanager_secret.score_api_db_url` → `aws_secretsmanager_secret.score_api["database_url"]`
- Secret name path changes: `world-score-api/database-url` → `worlds/score-api/database_url`
- Generator does NOT emit `aws_secretsmanager_secret_version` (which contains the templated postgres URL from RDS resources). The schema doesn't model "secret value templated from other terraform resources" — score-api's pattern is unique among the 5 worlds in that the secret VALUE is computed in terraform, not loaded later by a script.

**Resolution path**:
- Option A: `terraform state mv aws_secretsmanager_secret.score_api_db_url 'aws_secretsmanager_secret.score_api["database_url"]'` + accept new naming convention; manually re-populate the secret_version with the postgres URL.
- Option B: Add a `secret_value_template` field to the schema for templated values; teach generator to emit `aws_secretsmanager_secret_version` when present. Backward-fits score-api into the canonical pattern.
- Option C: Keep score-api's `world-score-api.tf` hand-edited; flag as `generator: skip` in the YAML manifest.

**Recommended**: Option B for the schema (additive v1.1 minor bump; field is `templated_value` on `SecretDeclaration`). Land before adopting generator for score-api.

### 2. mibera secret naming convention (MEDIUM IMPACT)

**Live**: `arrakis/${var.environment}/worlds/honeyroad/${each.key}` and resource named `aws_secretsmanager_secret.honeyroad`.

**Generated**: `${local.name_prefix}/worlds/mibera/${each.key}` and resource named `aws_secretsmanager_secret.mibera`.

**Differences**:
- Resource address: `honeyroad` → `mibera` (state migration needed).
- Path segment: `honeyroad` → `mibera` (different secrets path; would create new secrets and orphan old).
- Top-level: `arrakis/${var.environment}/...` → `${local.name_prefix}/...` (these resolve to the same string since `name_prefix = "arrakis-${var.environment}"` minus a slash, but the rendered path differs).

**Resolution path**:
- Option A: `terraform state mv` for resource address + accept new naming. Operator needs to re-load secrets via `scripts/load-honeyroad-secrets.sh` (script reads the live secrets and rewrites under new name).
- Option B: Add `secrets_resource_alias` to manifest (`mibera.yaml` declares `secrets_resource_alias: honeyroad` so generator emits `aws_secretsmanager_secret.honeyroad`).
- Option C: Rename the live resources to match canonical form (one-time migration, then forever-clean).

**Recommended**: Option C. The "honeyroad" naming was a B2-migration artifact (mibera-world repo swapped to mibera-honeyroad mid-cycle). Slug is `mibera`; path should be `worlds/mibera/`. One-time `terraform state mv` + load script update.

### 3. midi secret naming path (LOW IMPACT)

**Live**: `arrakis/${var.environment}/worlds/midi/${each.key}` (already canonical-aligned).

**Generated**: `${local.name_prefix}/worlds/midi/${each.key}`.

**Differences**: Top-level path-segment style only. `arrakis/${var.environment}` resolves to `arrakis-${var.environment}` (close but with `/` vs `-`). Need to confirm `local.name_prefix` definition in loa-freeside.

**Resolution path**: Confirm `name_prefix` value; either align generator output to match `arrakis/${var.environment}` style, or accept the canonical form with one-time secret recreation.

**Recommended**: Inspect `loa-freeside/main.tf` (or wherever `local.name_prefix` is defined) and decide. Likely a minor naming-convention reconciliation.

---

## Path to zero-diff

To get the existing 5 worlds onto the generator without resource recreation:

1. **schema v1.1 (additive)**: add `templated_value` field on `SecretDeclaration` to model score-api's RDS-templated postgres URL. Add `secrets_resource_alias` for mibera's `honeyroad` legacy name (or migrate).
2. **Reconcile name_prefix path style**: confirm whether canonical is `arrakis-${env}/worlds/{slug}/{key}` or `arrakis/${env}/worlds/{slug}/{key}`. Update generator to match.
3. **One-time terraform state migration** for mibera (honeyroad → mibera resource rename) and score-api (score_api_db_url → score_api["database_url"]).
4. **Re-run generator + diff**: cosmetic-only. Adopt as source-of-truth.

Estimated effort: 1-2 days. Tracked as follow-up in the design doc's open questions.

---

## What works today

- ✅ All 5 YAMLs validate against `world-manifest.schema.json` v1.0
- ✅ Generator emits all 5 `.tf` files + 3 `-secrets.tf` files
- ✅ apdao + rektdrop are cosmetic-only diffs (safe to adopt now)
- ✅ Schema covers every field used by the live infrastructure
- ✅ The pattern works — net-new worlds (created from this point forward) will go through the generator and be canonical from day 1

The semantic diffs are migration debt for the 5 historical worlds, not a flaw in the generator. New worlds are generator-first; historical worlds need 1-2 days of state-migration work to converge.

---

## Recommendation

For the **MVP scope**: ship the generator with the v1.0 schema + 5 YAMLs as documented. Adopt the generator for **net-new worlds only** (instance-6+). Plan a follow-up cycle for state-migration of mibera + score-api + midi onto the canonical form. apdao + rektdrop can adopt the generator output any time.

This is the honest reading of the gate. The pattern is sound; the 5 historical worlds carry idiosyncratic naming that needs reconciliation work the generator design surfaced.
