{
  "name": "@freeside-worlds/config-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The config SERVICE — minimal Bun HTTP exposing GET/PUT /v1/config/:world/:surface over the config engine. The FIRST runtime in freeside-worlds. Auth is stubbed (service-token read + Bearer-placeholder write); real CM auth is C-2.",
  "license": "MIT",
  "main": "src/server.ts",
  "scripts": {
    "start": "bun src/server.ts",
    "test": "bun test"
  },
  "dependencies": {
    "@freeside-worlds/config-adapters": "workspace:*",
    "@freeside-worlds/config-engine": "workspace:*",
    "@freeside-worlds/config-protocol": "workspace:*",
    "yaml": "^2.6.0"
  },
  "peerDependencies": {
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
